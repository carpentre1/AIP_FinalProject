using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BunnyBehavior : MonoBehaviour
{

    //Bunny: Avoids all other animals upon sight
    //Avoids the scent of foxes
    //Moves to and drinks water it sees
    //Moves to and eats foliage it sees
    //Remembers where it last saw a fox and avoids that area
    //Runs away from any loud sound
    //Stops eating/drinking momentarily if it hears a quiet sound

    ////States: wandering, running from predator, moving to water, moving away from fox area


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
