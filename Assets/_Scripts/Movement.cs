using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Movement : MonoBehaviour
{

    public float movementSpeed = 1.0f;//how fast the animal moves by default
    public float walkMultiplier = 1.0f;
    public float sprintMultiplier = 2.0f;

    public float waterMultiplier = .5f;//how much they're slowed down by water
    public float foliageMultiplier = .5f;//how much they're slowed down by foliage
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
