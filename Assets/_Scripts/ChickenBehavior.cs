using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ChickenBehavior : MonoBehaviour
{

    //Chicken: Follows the scent or sight of other chickens, tries to group up
    //Remembers where it last saw a group of other chickens and tries to return there when doing nothing else
    //Moves to and drinks water it sees
    //Moves to and eats foliage it sees
    //Avoids foxes when seen
    //Doesn't pay any attention to sound because they're stupid chickens

    //States: wandering, running from predator, moving to water, returning to remembered chicken area

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
